@model Shopping.MVCWebUI.Entity.Product

@{
    ViewBag.Title = "Delete";
    Layout = "/Views/Shared/_AdminLayout.cshtml";
}


    <h2>Bu ürünü silmek istediğinizden emin misiniz ?</h2>
    <hr />

    <div>

        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>
        </dl>
        <hr />

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value=" Sil " class="btn btn-danger btn-sm" /> |
                <a href="/Product/Index" class="btn btn-warning btn-sm"><i class="fas fa-undo" style="font-size: 13px;"></i> Listeye dön</a>
            </div>
        }
    </div>
